#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../monkey-proxy/bin/proxy" "$@"
  ret=$?
else 
  node  "$basedir/../monkey-proxy/bin/proxy" "$@"
  ret=$?
fi
exit $ret
